---
- name: RabbitMQ Installation/Uninstallation Playbook
  hosts: lemp_servers
  become: yes
  gather_facts: yes
  
  vars:
    rabbitmq_action: "{{ action | default('install') }}"
  
  roles:
    - role: rabbitmq
      when: rabbitmq_action in ['install', 'uninstall']
